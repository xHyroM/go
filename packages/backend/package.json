{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "shards build",
    "dev": "shards run",
    "install": "shards install",
    "test": "KEMAL_ENV=test crystal spec",
    "lint": "crystal tool format"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {}
}
