{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "run": "./bin/hypilus",
    "build": "shards build",
    "dev": "shards run",
    "install": "shards install",
    "test": "KEMAL_ENV=test crystal spec",
    "lint": "crystal tool format"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {}
}
